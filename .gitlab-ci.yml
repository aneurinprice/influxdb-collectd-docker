stages:
  - build

build:
  stage: build
  tags:
    - generic 
  image:
    name: moby/buildkit:rootless
  variables:
    DOCKER_BUILDKIT: 1
  script:
     - |
        buildctl-daemonless.sh build \
            --frontend=dockerfile.v0 \
            --local context=. \
            --local dockerfile=.
